// Eyebrow with leading halo gradient bar — see `.eyebrow` in globals.css.
export function Eyebrow({ children }: { children: React.ReactNode }) {
  return <span className="eyebrow">{children}</span>
}
